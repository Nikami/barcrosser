{
  "name": "barcrosser",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "web",
    "plugin",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently --names \"WEB,PLUGIN\" --prefix-colors \"cyan,green\" \"npm run dev -w web\" \"npm run dev -w plugin\"",
    "dev:web": "npm run dev -w web",
    "dev:plugin": "npm run dev -w plugin",
    "test": "npm run test -w web",
    "build": "npm run build -w shared && npm run build -w web && npm run build -w plugin",
    "build:web": "npm run build -w web",
    "build:plugin": "npm run build -w plugin",
    "lint": "concurrently \"npm run lint -w web\" \"npm run lint -w plugin\"",
    "deploy": "vercel --prod"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "vercel": "^41.0.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}